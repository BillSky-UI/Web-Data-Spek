/// Informasi hasil penyimpanan file ke lokasi tujuan per platform.
class SavedTarget {
  final bool ok;
  final String path;
  final String location;
  const SavedTarget(this.ok, this.path, this.location);
}